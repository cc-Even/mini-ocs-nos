[build-system]
requires = ["hatchling==1.27.0"]
build-backend = "hatchling.build"

[project]
name = "mini-ocs-nos"
version = "0.1.0"
description = "Management-plane services and CLI for mini-ocs-nos"
requires-python = ">=3.12,<3.13"
dependencies = [
    "grpcio==1.74.0",
    "grpcio-tools==1.74.0",
    "protobuf==6.31.1",
    "pydantic==2.11.7",
    "redis==6.4.0",
    "typer==0.16.0",
]

[project.scripts]
gnmi-server = "gnmi_server.server:main"
ocsctl = "ocsctl.main:app"

[dependency-groups]
dev = [
    "pytest==8.4.1",
    "pytest-asyncio==1.1.0",
    "ruff==0.12.9",
]

[tool.hatch.build.targets.wheel]
packages = ["python/github", "python/gnmi_server", "python/ocsctl"]

[tool.hatch.build.targets.wheel.force-include]
"proto/github.com/openconfig/gnmi/LICENSE" = "gnmi_server/licenses/openconfig-gnmi-LICENSE"

[tool.pytest.ini_options]
addopts = "-ra --strict-config --strict-markers"
testpaths = ["python/tests"]
asyncio_mode = "auto"

[tool.ruff]
target-version = "py312"
line-length = 100
extend-exclude = ["python/github"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
